const { sub, add, name } = require("./operator");
console.log(add(10, 5));
console.log(sub(10, 5));
console.log(name);
//  In object destructuring we have to give the real variable or function name
// In react during destructuring the names has to givn in order
